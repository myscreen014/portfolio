tinymce.PluginManager.add('file', function(editor, url) {
    // Add a button that opens a window
    editor.addButton('file', {
        title: 'Insert file/image',
        icon: 'image',
        id: editor.id+'-upload-clickable',
        onclick: function() {}
    });
}); 